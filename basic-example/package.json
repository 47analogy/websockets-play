{
	"name": "socket-server",
	"version": "1.0.0",
	"description": "Playing around with websockets. Add some tests and do some experimenting.",
	"main": "server.js",
	"scripts": {
		"start": "nodemon server.js",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"author": "",
	"license": "MIT",
	"devDependencies": {
		"nodemon": "^1.18.10"
	},
	"dependencies": {
		"cors": "^2.8.5",
		"express": "^4.16.4",
		"socket.io": "^2.2.0"
	}
}
